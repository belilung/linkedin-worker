[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = auto
markers =
    unit: unit tests (no I/O)
    integration: integration tests (disk, subprocess)
    slow: slow tests
filterwarnings =
    ignore::DeprecationWarning
